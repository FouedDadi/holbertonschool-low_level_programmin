#include "holberton.h"
/**
 *more_numbers- print all numbers from 0 to 4 10 times
 *
 */
void more_numbers(void)
{
int x, y;
for(y = 0; y < 10; y++)
{ 
for (x = 0; x <= 14; x++ )
{
if(x > 9)
{
_putchar((x / 10) + '0');
_putchar((x % 10) + '0');
}
else
{
_putchar(x + '0');
}
_putchar('\n');
}
}
