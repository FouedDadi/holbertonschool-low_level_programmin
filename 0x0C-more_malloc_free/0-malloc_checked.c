#include "holberton.h"
#include <stdlib.h>
#include <stdio.h>
/**
 **malloc_checked- allocate memory for b and check if the function fails
 *@b: unsigned integer
 */
void *malloc_checked(unsigned int b)
{
int *x;
x = malloc(unsigned int b);
if (w == NULL)
exit(98);
}
