#include <stdio.h>
/**
 *main - Entry point
 *execpt q & e
 *Return: Always 0 (Success)
 */
int main(void)
{
char x;

for (x = 'a'; x <= 'z'; x++)
{
if (x != 'q' && x != 'e')
{
putchar(ch);
}
}
putchar('\n');
return (0);
}
