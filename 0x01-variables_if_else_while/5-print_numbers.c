#include <stdio.h>
/**
 *main - print all digit numbers of base 10
 *Return: Always 0 (Success)
 */
int main(void)
{
int x = 0;
for (x = 0; x <= 9; x++)
{
printf("%d", x);
}
printf("\n");
return (0);
}

